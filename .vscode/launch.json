{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Forge",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "build",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/../filter-samples"
      ]
    }
  ]
}
